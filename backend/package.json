{
  "name": "roulotte-online-backend",
  "version": "1.0.0",
  "description": "Backend for roulotte.online",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "node -c server.js && node -c db.js && node -c init-db.js && node -c s3-client.js && node -c test/api.test.js",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.957.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "meilisearch": "^0.45.0",
    "multer": "^1.4.5-lts.1",
    "pino-http": "^10.5.0",
    "pg": "^8.11.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
